# Migration `20200129200854-add-light-instruction`

This migration has been generated by Michael Hayes at 1/29/2020, 8:08:54 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."new_Plant" (
    "acquiredAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,
    "commonName" TEXT NOT NULL DEFAULT '' ,
    "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,
    "latinName" TEXT NOT NULL DEFAULT '' ,
    "lightInstructions" TEXT NOT NULL DEFAULT '' ,
    "location" TEXT NOT NULL DEFAULT '' ,
    "waterInstructions" TEXT NOT NULL DEFAULT '' 
) 

INSERT INTO "quaint"."new_Plant" ("acquiredAt", "commonName", "id", "latinName", "location", "waterInstructions") SELECT "acquiredAt", "commonName", "id", "latinName", "location", "waterInstructions" FROM "quaint"."Plant"

DROP TABLE "quaint"."Plant";

ALTER TABLE "quaint"."new_Plant" RENAME TO "Plant";

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200129184414-init..20200129200854-add-light-instruction
--- datamodel.dml
+++ datamodel.dml
@@ -6,13 +6,13 @@
 // The `url` field must contain the connection string to your DB.
 // Learn more about connection strings for your DB: https://pris.ly/connection-strings
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "sqlite:./dev.db"
 }
 // Other examples for connection strings are:
-// SQLite: url = "***"
-// MySQL:  url = "***"
+// SQLite: url = "sqlite:./dev.db"
+// MySQL:  url = "mysql://johndoe:johndoe@localhost:3306/mydb"
 // You can also use environment variables to specify the connection string: https://pris.ly/prisma-schema#using-environment-variables
 // By adding the `generator` block, you specify that you want to generate Prisma's DB client.
 // The client is generated by runnning the `prisma generate` command and will be located in `node_modules/@prisma` and can be imported in your code as:
@@ -32,8 +32,9 @@
   commonName String
   latinName  String
   acquiredAt DateTime
   waterInstructions String
+  lightInstructions String
   location String
   notes Note[]
 }
```


